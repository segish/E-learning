<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>forgot password</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: #23242a;
        }

        .box {
            position: absolute;
            left: 35%;
            top: 20%;
            width: 380px;
            height: 450px;
            background: #1c1c1c;
            border-radius: 8px;
            overflow: hidden;
            visibility: hidden;
        }

        .box.change.reset {
            visibility: visible;
        }

        .box.success.successed {
            width: 380px;
            height: 250px;
            padding-top: 6%;
            padding-left: 5%;
            color: rgb(19, 184, 19);
            visibility: visible;
        }

        .box.expire.error {
            width: 380px;
            height: 250px;
            padding-top: 6%;
            padding-left: 5%;
            color: red;
            visibility: visible;
        }

        .box form {
            position: absolute;
            inset: 4px;
            background: #222;
            padding: 50px 40px;
            border-radius: 8px;
            display: flex;
            flex-direction: column;
        }

        .box form h2 {
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
            color: #fff;
            font-weight: 500;
            text-align: center;
            letter-spacing: 0.1rem;
            display: flex;
            flex-direction: column;
        }

        .box form .inputBox {
            position: relative;
            width: 300px;
            margin-top: 35px;
        }

        .box form .inputBox input {
            position: relative;
            width: 100%;
            padding: 20px 10px 10px;
            background: transparent;
            outline: none;
            border: none;
            box-shadow: none;
            color: #23242a;
            font-size: 1em;
            letter-spacing: 0.05em;
            transition: 0.5s;
            z-index: 10;
        }

        .box form .inputBox span {
            position: absolute;
            left: 0;
            pointer-events: none;
            color: #8f8f8f;
            padding: 20px 0px 10px;
            font-size: 1em;
            letter-spacing: 0.05em;
            transition: 0.5s;
        }

        .box.change.reset form .inputBox h6 {
            position: absolute;
            left: 0;
            width: 100%;
            color: red;
            visibility: hidden;
        }

        .box.change.reset.incorect form .inputBox h6 {
            visibility: visible;
        }

        .box form .inputBox input:valid~span,
        .box form .inputBox input:hover~span,
        .box form .inputBox input:focus~span {
            color: #fff;
            font-size: 0.75em;
            transform: translateY(-34px);
        }

        .box form .inputBox i {
            position: absolute;
            left: 0;
            bottom: 0;
            width: 100%;
            height: 2px;
            background: #ff5f0e;
            border-radius: 4px;
            overflow: hidden;
            transition: 0.5s;
            pointer-events: none;
        }

        .box form .inputBox input:valid~i,
        .box form .inputBox input:hover~i,
        .box form .inputBox input:focus~i {
            height: 44px;
            background: #fff;
        }

        .box form .submit {
            position: relative;
            padding: 3px;
            margin-left: 32%;
            margin-top: 40px;
            width: 105px;
            height: 40px;
            overflow: hidden;
            border-radius: 6px;
        }

        .box form input[type="submit"] {
            border: none;
            outline: none;
            padding: 9px 25px;
            background: #ff5f0e;
            cursor: pointer;
            border-radius: 6px;
            font-weight: 600;
            font-size: 0.9em;
            width: 100px;
            z-index: 2;
        }

        .box form input[type="submit"]:active {
            opacity: 0.8;
        }

        .box form input[type="submit"]:hover {
            background: #ff5f0e;
        }

        .hidden {
            visibility: hidden;
        }

        a {
            text-decoration: none;
            color: azure;
            margin-left: 20px;
        }

        a:hover {
            color: rgb(244, 103, 9);
        }
    </style>
</head>

<body>
    <div class="box change <%= status %>">
        <form action="" method="post">
            <h2 class="reset">provide new password</h2>
            <div class="inputBox">
                <input type="password" required="true" name="password" id="password" placeholder="password" onchange={validate}>
                <i></i>
            </div>
            <div class="inputBox">
                <input type="password" required="true" name="coniform_password" id="coniform_password"
                    placeholder="coniform password" onclick={validate}>
                <i></i>
                <h6><%=message%></h6>
            </div>
            <div class="submit">
                <input type="submit">
            </div>
        </form>
    </div>
    <div class="box success <%= status %>">
        <h2>
            <%=message%>
        </h2>
        <a href="http://localhost:3000/Login">click here to goto login page</a>
    </div>
    <div class="box expire <%= status %>">
        <h2>
            <%=message%>
        </h2>
        <a href="http://localhost:3000/Forgot">click here to resend email</a>
    </div>
    <script src="/javascript.js"></script>
</body>

</html>